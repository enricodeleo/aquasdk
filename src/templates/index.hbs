/**
* {{apiInfo.title}} - SDK
* {{apiInfo.description}}
*
* Version: {{apiInfo.version}}
* Generated: {{timestamp}}
*/

import ApiClient from './client.js';
{{#each resources}}
import {{#ifEquals strategy 'hierarchical'}}{{camelCase name}}{{else}}{{pascalCase name}}Resource{{/ifEquals}} from './resources/{{camelCase name}}.js';
{{/each}}

export class SDK {
  constructor(config = {}) {
    this.client = new ApiClient(config);

    {{#each resources}}
    this.{{camelCase name}} = {{#ifEquals strategy 'hierarchical'}}{{camelCase name}}(this.client){{else}}new {{pascalCase name}}Resource(this.client){{/ifEquals}};
    {{/each}}
  }


  setToken(token) {
    this.client.setToken(token);
    return this;
  }

  setBasicAuth(username, password) {
    this.client.setBasicAuth(username, password);
    return this;
  }

  setHeader(name, value) {
    this.client.setHeader(name, value);
    return this;
  }
}

export default SDK;
